.container-narrow
  %section
    %h2 Место проведения

    %p
      Конференц-зал отеля Рейкарц
      %br
      ул. Героев Сталинграда, 22

    %p
      %strong Начало регистрации: 15 июня в 9:30


  #map-canvas

= content_for :javascript do
  :javascript

    var map;
    function initializeMap() {
      var mapOptions = {
        zoom: 17,
        center: new google.maps.LatLng(44.582415,33.434897),
        mapTypeId: google.maps.MapTypeId.ROADMAP
      };
      map = new google.maps.Map(document.getElementById('map-canvas'),
          mapOptions);
    }

    google.maps.event.addDomListener(window, 'load', initializeMap);
